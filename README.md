# AutoSQL
AutoSQL is an AI-powered Python application that converts natural language into executable MySQL commands using Google's Gemini API. It allows users to interact with databases conversationally, making SQL operations faster and easier without manually writing queries.
